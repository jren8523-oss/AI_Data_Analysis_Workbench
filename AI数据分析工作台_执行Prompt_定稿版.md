# AI 数据分析工作台（课堂演示版）—— 产品需求与执行 Prompt（定稿版）

> 版本：v2.2 定稿 · 用途：交给任何 AI 工具即可照着搭建，无需再澄清需求
> 适用课程：《统计与数据分析基础》（人民邮电出版社）
> 风格来源：**老师指定的两个参考 PPT**——《AI快速高质量做数据分析》《10分钟AI高质量数据分析》
> 定位声明：工作台是**独立工具**，与十个章节课程 PPT 无绑定关系；
> 十个课程实训仅作为**验收手段 / 能力举例**（验证工作台能否完成对应任务）

---

## 0. 你的角色

你是本项目的**产品架构师 + AI Agent 工程师 + 数据分析师 + 测试工程师**。
最终验收标准只有一个：

> **这套工作台能否让 9 类预设任务真正跑通（另 1 类任务因外部工具限制明确占位）。**

课程 PPT 中的知识、术语、实训要求是**第一优先级依据**；没有依据时不擅自改变
课程统计方法、概念或任务要求；课程没说明的内容可用通用知识补充，但必须区分：

- 【课程材料明确要求】
- 【系统设计推断】
- 【通用数据分析能力】

---

## 一、项目目标与形态（已定稿，不要改）

做一个**课堂演示用的 AI 数据分析工作台**（独立工具）：

- 老师给出「一份数据 + 一个任务类型」→ 工作台自动跑对应实训全流程
  → 输出 HTML 演示报告；**可选**导出「交付 PPT」（分析结果的展示 PPT，可编辑）
- 与十个章节课程 PPT 无关：工作台是独立交付物，十个实训只是验收手段/能力举例
- **形态：静态 HTML 工作台**。双击 `index.html` 即开，完全离线，无后端，无需联网
- **首页 = 仿参考 PPT 封面风格**：顶部「AI DATA ANALYSIS」小标签 + 大标题
  + 3 个卖点卡片 + 10 张预设任务卡片（5×2 网格，人邮蓝 `#005DA2` 主题，微软雅黑字体）
- **任务 02（八爪鱼网页采集）无法实现 → 灰色占位卡片**：
  图标 + 文字「需外部采集工具，暂不可用」，不生成报告、不伪造 PASS
- 其余 **9 个任务**：每任务内置一份【示例数据】（按课程文字描述构造，报告内标注构造口径），
  跑通「数据概况 → 分析计算 → 图表 → 分层结论 → QA 验证」全流程，输出 1 份 HTML 报告
- **可选输出「交付 PPT」**：每个任务的分析结果可一键导出为可编辑 PPTX
  （封面 + 任务说明 + DIG 过程 + 图表 + 结论 + 讲稿要点），供结果汇报使用，
  与课程 PPT 无关；对齐参考 PPT-A「报告沉淀成可编辑 PPT」、PPT-B「最终交付」理念
- 教师可把自己的数据替换到 `data/raw/` 同名 CSV，重跑同一流程，无需改代码

---

## 二、核心原则（不可违背）

1. **实训驱动**：每个能力必须能追溯到至少一个课程实训
2. **先理解数据，再分析**：禁止「上传数据 → 直接生成结论」；
   必须 Describe → Inspect → Goal → Analyze → Verify → Visualize → Report
3. **LLM 不做心算**：统计量/区间估计/回归/指数/时间序列一律用确定性代码
   （pandas / numpy / scipy / matplotlib）计算；LLM 只负责任务理解、方法选择、结果解释
4. **原始数据永远只读**：任何处理都产生新文件；QA 校验原始文件哈希未被修改
5. **所有计算可追溯**：每个指标必须带 公式 → 代入 → 结果 → 解释 四件套
6. **不允许伪造结果**：数据不足就明确告知；做不了的任务就占位（如任务 02）
7. **相关 ≠ 因果**：任何相关/回归分析报告必须出现警示框
8. **图表与正文同源取数**：图表和正文都从同一份指标 dict 渲染，杜绝数字不一致

---

## 三、工作台整体架构

```
                         AI 数据分析工作台（静态 HTML）
                                  │
                           ┌──────▼──────┐
                           │ 首页 index   │  ← 10 张任务卡片
                           └──────┬──────┘
                                  │
             ┌────────────────────┼────────────────────┐
             ↓                    ↓                    ↓
        Python 计算引擎        任务编排脚本           HTML 报告
       engine/*.py           tasks/taskNN_*.py     output/taskNN/
             │                    │                    │
   数据导入/清洗/描述统计  每任务只调引擎不写算法   16:9 截图友好
   抽样/指数/回归/时序    （薄编排层）             讲解要点+QA
   图表工厂（PNG）
```

数据流：

```
data/raw/*.csv  →  describe 数据概况  →  指标 dict
   （只读）          （规模/字段/缺失/分布）   （每项含 value/formula/
                                                substitution/interpretation）
        ↓                                            ↓
   viz.py 出 PNG（16:9, dpi=150）          report.py 拼装 HTML
        ↓                                            ↓
   charts/*.png  ────────────────→  output/taskNN/report.html
                                             ↑
                                     qa.py 三层复核写入报告尾部
```

---

## 四、目录结构（按此搭建）

```
AI_Data_Analysis_Workbench/
├── index.html                  # 10 任务卡片首页（02 灰卡占位）
├── README.md                   # 分享说明：示例数据声明、一键生成、打开方式
├── 一键生成全部报告.bat        # 双击运行全量构建
├── assets/css/workbench.css    # 人邮蓝主题、16:9 区块样式
├── data/
│   ├── raw/*.csv               # ★只读示例数据（9 个，UTF-8-sig）
│   └── processed/              # 引擎产物（清洗结果、透视表导出等）
├── engine/                     # 确定性计算引擎（纯函数式）
│   ├── config.py               # 常量/路径/任务元数据/主题色
│   ├── fmt.py                  # 数字格式化（千分位/百分比/防浮点脏值）
│   ├── io_utils.py             # CSV 读写（UTF-8-sig/GBK 兼容）、raw 只读守卫
│   ├── describe.py             # 数据概况 describe()
│   ├── stats_core.py           # 描述统计（集中趋势/离散程度/分布形态）
│   ├── stats_infer.py          # 抽样估计（任务 05）
│   ├── stats_index.py          # 统计指数 + 因素分析（任务 06）
│   ├── stats_corr.py           # 相关回归（任务 07）
│   ├── stats_ts.py             # 时间序列（任务 08）
│   ├── viz.py                  # matplotlib 图表工厂（中文、16:9、PNG）
│   ├── report.py               # HTML 渲染（骨架+区块+讲解要点）
│   ├── ppt_out.py              # 可选输出：交付 PPT 导出器（python-pptx，16:9 可编辑）
│   ├── qa.py                   # 三层验证
│   └── pipeline.py             # 流水线编排 + build_log
├── tasks/taskNN_*.py           # 9 个任务薄编排脚本（只调引擎，不写算法）
├── templates/report_skeleton.html
├── scripts/run_all.py          # 全量生成 + output/_build/build_log.json
└── output/taskNN/report.html   # 每任务 1 份演示报告 + charts/*.png + 可选 report.pptx
```

---

## 五、10 个任务规格表（术语对齐教材，勿改）

> 说明：这 10 个任务对应课程 10 个课堂实训，作为工作台的**能力验收清单 / 举例**；
> 工作台本身不依赖课程 PPT，教师可换任意同类数据重跑同一流程。

### 任务 01 · 店铺访客数分析【可跑】
- 数据 `task01.csv`：月份 × 5 个访客来源类目 + 总访客数，12 行
- 指标：总访客数、类目占比、环比增速、月度均值/峰值
- 图表：柱形图（各月总访客）、折线图（趋势）、饼图（类目占比）
- 特色：三种分析思维（对比 / 趋势 / 占比）各配一张图

### 任务 02 · 使用八爪鱼采集招聘数据【占位】
- 灰色卡片，图标 + 「需外部采集工具，暂不可用」
- 不做、不伪造、不参与构建；可在首页注明「导入采集结果 CSV 后即可接入分析」

### 任务 03 · 数据清洗与加工【可跑】
- 数据 `task03.csv`：约 60 行，含缺失值 / 错误日期 / 文本型数字 / 异常值 / 重复行 / 脏名称
- 指标：清洗规则全记录（每条规则：字段 → 问题 → 处理 → 处理后样例）；
  清洗后各行业 均值 / 标准差 / 波动系数（=标准差/均值）/ 极差
- 图表：清洗前后对比柱形图、原始→规则→清洗后三态对照表
- 特色：**禁止静默修改**，必须展示「原始数据 → 清洗规则 → 清洗后数据」

### 任务 04 · 生产资料市场价格分析【可跑】
- 数据 `task04.csv`：1–5 月 × 4 个生产资料品种的价格
- 指标：
  - 集中趋势：均值 / 中位数 / 众数（取最小众数，与 Excel MODE 一致）
  - 离散程度：极差 / 方差（样本无偏） / 标准差 / 四分位差 / 平均差 / 变异系数
  - 分布形态：偏度 / 峰度（scipy，无则 pandas 退化实现）
- 图表：直方图+箱线图、价格折线图
- 特色：**每个指标都给「公式 → 代入 → 结果 → 解释」**，不只输出数字

### 任务 05 · 小区居民用电分析【可跑】
- 数据 `task05.csv`：100 户居民电费（示例数据按课程描述构造：约 30–40% 户 > 80 元）
- 指标（95% 置信水平，大样本 n=100，z=1.96）：
  - 总体均值区间估计：`x̄ ± z·(s/√n)`
  - 总体比例区间估计（电费 > 80 元的比例）：`p̂ ± z·√(p̂(1−p̂)/n)`
  - 大样本判定 n ≥ 30；给出区间上下限
- 图表：直方图 + 均值线 + 置信区间标注、比例饼图（>80 元 / ≤80 元）
- 特色：展示完整链条 **输入 → 统计量 → 公式 → 计算过程 → 结果 → 解释**

### 任务 06 · 产品总成本变动分析【可跑】
- 数据 `task06.csv`：3 种产品的 产量(q0,q1) 与 单位成本(p0,p1)
- 指标：个体指数、综合指数（拉氏指数 / 帕氏指数）、平均指数
  （加权平均指数 / 调和平均指数）、指数体系自洽校验、因素分析
  （总成本变化 = 产量变动影响 × 单位成本变动影响）
- 图表：分组柱形图（q0/q1 或 p0/p1）、指数对比图
- 特色：**总体变化 → 各因素 → 各因素影响 → 综合结论**，指数体系必须自洽

### 任务 07 · 研发与销售关系分析【可跑】
- 数据 `task07.csv`：20 次抽样（研发投入 x / 销售额 y，示例数据须保证 R² > 0.8
  且预测点落在样本范围内）
- 指标：相关系数 `r = Σ(x−x̄)(y−ȳ) / √[Σ(x−x̄)²·Σ(y−ȳ)²]`、相关方向与程度、
  一元线性回归 `ŷ = a + bx`（最小二乘）、R²、拟合优度、预测 x=15 万元时的销售额
- 图表：散点图 + 回归线 + 预测点标注
- 特色：**必须出现「相关 ≠ 因果」警示框**，并在结论中提示不可据此断言因果

### 任务 08 · 分析并预测企业总产值【可跑】
- 数据 `task08.csv`：15 年企业总产值（2010–2024，示例数据须为上行趋势）
- 指标：平均发展水平、平均增长量、环比发展速度、定基发展速度、
  环比增长速度、定基增长速度、平均发展速度（几何平均）、平均增长速度、
  线性趋势方程（最小二乘参数 a/b）、预测 2025 年总产值
- 图表：折线图 + 趋势线 + 2025 预测点
- 特色：必须提示「外推预测的适用范围」——趋势外推假设条件在样本外可能失效

### 任务 09 · 网店访客数可视化展现【可跑】
- 数据 `task09.csv`：7 天 × 6 个商品类目 的长表（42 行）
- 能力：统计表、排序、分类汇总、柱形图、折线图、饼图、数据标签、
  图表标题、**数据透视表（导出 CSV 到 data/processed/）**、数据透视图
- 图表：日访客柱形图、趋势折线图、类目占比饼图、透视表热力/分组图
- 特色：与任务 01 区分——01 讲「分析思维」，09 讲「可视化技术手段」

### 任务 10 · 制作销售数据分析报告【可跑】
- 数据 `task10.csv`：近 3 年 × 12 月 × 3 办事处 销售数据（约 108 行）
- 流程：分年度分析 → 年度之间比较 → 三年总体汇总 → 提炼核心发现 → 生成图表
  → 形成完整数据分析报告
- 图表：年度对比柱形图、3 系列月度折线图、办事处占比饼图
- 特色：综合全部能力的「完整报告成品」，结论与数据一一对应

---

## 六、参考 PPT 风格规范（老师指定，必须对齐）

工作台视觉与内容框架**必须对齐**老师指定的两个参考 PPT（《AI快速高质量做数据分析》
《10分钟AI高质量数据分析》，各 13 页，位于课程目录，仅作风格参考，不改动原件）。
提取的风格规范如下：

### 6.1 内容框架（必须采用）
- **Agent 工作流**：需求 → 数据 → 技能 → 产出（参考 PPT-A 核心流水线）
- **DIG 分析框架**：D Describe 描述数据 / I Inspect 挖掘数据 / G Goal 给出产出（参考 PPT-B 核心）
- **报告逻辑**：Why 为什么 → What 哪里 → How 怎么优化
- **五步法**：找 → 说 → 验 → 挖 → 交
- 每个分析步骤配「推荐 Prompt」框（老师可直接复制使用）

### 6.2 视觉要素（每页 / 每屏）
- 页头：顶部 10pt 灰色小标签「AI DATA ANALYSIS」+ 27pt 大标题（人邮蓝深色）
- 结构化卡片：编号步骤（1/2/3…+ 箭头）、A/B/C 对比块、大字母块（D/I/G、Why/What/How）
- 「推荐 Prompt」引述框（浅色底 + 标签）
- 底部小结条：每屏最后一行总结句（讲解要点金句）
- 16:9 区块（13.33×7.5 比例），整屏截图后与参考 PPT 同风格

### 6.3 应用位置
- **首页**：仿参考 PPT 封面——小标签 + 大标题 + 3 卖点卡片 + 10 任务卡片
- **任务报告页**：按 §七 区块序，一屏一块、块内卡片化布局
- **结论块**：仿参考 PPT 结论页的 3 卡片收尾（如 task10 的 核心发现三卡片）
- **交付理念（对齐 PPT-B 第 11 页「最终交付」）**：分析产物不止是文字——
  图表、透视表、报告都可下载可编辑；「交付 PPT」为可选输出（见 §七 末）

### 6.4 推荐 Prompt 框的定位（重要）
- 推荐 Prompt 是**给学生课后自己跑 AI 复现同款分析**用的提示词
- 工作台报告 = 该 Prompt 的**预期产出 / 标准答案**
- 因此 Prompt 必须**可独立拿走直接用**：包含 任务描述 + 数据要求 + 期望输出结构
  + 验收要点（学生可自行对答案），不能只是一句装饰性提示词

---

## 七、HTML 演示报告规范（每任务 1 份）

页面：`output/taskNN/report.html`，16:9 截图友好（容器 max-width 1280，一屏一块）。

统一区块序（每块附「讲解要点」灰底 callout；风格对齐参考 PPT，见 §六）：

```
顶部小标签「AI DATA ANALYSIS」+ 大标题（任务编号/章节/标题）+ 日期
示例数据横幅（⚠ 示例数据，非课程原始数据）
① 需求（任务说明）  — 一句话业务问题 + 数据来源 + 希望的结果（PPT-A 流水线起点）
② D 描述数据        — 数据概况：规模/字段/类型/缺失/唯一值/示例行
③ I 挖掘数据        — 公式与计算过程：编号步骤 + 公式→代入→结果→解释 四件套
④ 图表              — 柱/折/饼，每张图附：图题/图注/图表目的/核心发现
⑤ G 给出产出        — 分层结论：事实 / 分析 / 推测 / 建议，按 Why→What→How 组织
⑥ 推荐 Prompt       — 本任务「推荐 Prompt」引述框（老师可直接复制）
⑦ 警示框 + QA 验证   — 相关≠因果（task07 必现）、外推范围（task08）、三层验证绿标
底部小结条（讲解要点金句，对齐 PPT 每页底部小结）
页脚（QA 汇总：n/m 通过）
```

配色：人邮蓝 `#005DA2`（主）/ `#00447A`（深）/ `#E8F1F8`（浅底）/ `#F2A900`（强调）；
QA 通过绿 `#1E8E5A`、失败红 `#C0392B`；正文 `#2D3748`；背景 `#F4F7FA`。
字体：微软雅黑（matplotlib 用 `C:/Windows/Fonts/msyh.ttc`，fallback simhei）。

**可选输出「交付 PPT」（独立于课程 PPT）**：`engine/ppt_out.py` 用 python-pptx
把分析结果拼成可编辑 PPTX（16:9）：封面（任务名+章节）→ 任务说明 → D/I/G 过程 →
图表页（PNG 内嵌）→ 分层结论 → 讲稿要点页。文本全部可编辑、图表为图片可替换；
每任务在报告页提供「导出 PPT」入口/说明；阶段 3 实现，作为可选输出不阻塞验收。

---

## 八、QA 验证层（三层，全部写入报告尾部）

1. **数值复算**：每个核心指标配**独立复算函数**比对（用与主计算不同的代码路径/
   手写公式重算，避免同一 bug 被同一实现复制），容差 1e-6
2. **逻辑检查**：任务定制断言——
   - task05：n==100、z==1.96、lower < upper、比例区间在 [0,1]
   - task06：指数体系自洽（拉氏/帕氏/平均指数互相印证）
   - task07：−1 ≤ r ≤ 1、R² ≤ 1
   - task08：n==15、年份连续、趋势方向与数据一致
   - task01 vs task09 区分：task09 报告必须出现 排序表/分类汇总/透视表 CSV/透视图
     至少 2 项；task01 必须出现 环比增速 + 三种分析思维对照——二者图表集合不得相同
   - 全任务：原始数据文件 SHA-256 与构建前一致（未被修改）
3. **输出一致性**：图表与正文同源指标 dict；PNG 存在且非空；
   关键数字在 HTML 中出现 ≥ 1 次

失败处理：FAIL 记录进 `output/_build/build_log.json`，不阻断其他任务；修复后重跑全量。

---

## 九、实施顺序（分阶段，每阶段验收后再继续）

| 阶段 | 范围 | 验收标准 |
|---|---|---|
| 0 环境 | `pip install matplotlib scipy`；中文字体冒烟测试 | 中文 PNG 无乱码；scipy 可导入 |
| 1 样板 | 骨架 + 基础引擎 + 任务 01/03/04/09 跑通 | 4 份报告含全部标准区块+讲解要点，QA 全绿；**先交用户评审配色/版式/讲解风格（须对齐参考 PPT，见 §六）** |
| 2 统计专题 | stats_infer/index/corr/ts + 任务 05/06/07/08 | 公式口径与教材一致；task07 警示框；task08 外推说明 |
| 3 收尾 | 任务 10 + 首页 10 卡 + 交付 PPT 导出器（可选输出）+ README + .bat + 全量 QA | 9 报告 QA 全绿；交付 PPT 可生成且可编辑；无 Python 的电脑双击 index.html 全部可看 |

阶段 1 选 01/03/04/09 的理由：覆盖 描述统计 / 清洗 / 可视化 三套引擎能力，
并验证 01 与 09 的可区分性，是最小全链路验证集。

---

## 十、关键约束与风险对策

- **matplotlib 中文乱码** → font_manager 加载 msyh.ttc，fallback simhei；阶段 0 冒烟
- **众数语义** → 取最小众数（与 Excel MODE 一致）
- **示例数据不合理** → 构造后引擎自算校验（task05 比例 30–40%、
  task07 R²>0.8 且预测点在样本内、task08 趋势上行）
- **file:// 限制** → 全部相对路径，不使用 fetch / CDN / base64
- **CSV 中文** → 统一 UTF-8-sig（Excel 直开不乱码），读取兼容 GBK
- **误改原始数据** → io_utils 只读守卫 + QA 哈希校验
- **分享机器无 Python** → 报告/PPT 为静态产物可直接打开；README 注明只有「重新生成」才需要 Python，且**分享时整拷 `output/` 目录**（HTML + 图表 PNG + PPT）

---

## 十一、最终验收标准

交付《工作台验收报告》，至少包含：

```
一、系统架构
二、已实现能力
三、10 任务验收结果：
    task01 PASS / task02 占位(灰卡正确) / task03 PASS / task04 PASS
    task05 PASS / task06 PASS / task07 PASS / task08 PASS
    task09 PASS / task10 PASS
    （另：可选输出「交付 PPT」可用性验证——≥1 个任务导出 PPTX 可打开、可编辑）
四、每任务的：输入 / 执行过程 / 结果 / 验证结果
五、失败项及原因
六、当前系统限制（如 02 采集受限、外推预测范围）
七、下一步优化建议（如接 ECharts 交互、Streamlit 界面、真数据接入）
```

最终判断标准只有一句话：

> **这套工作台能否真正完成课程 PPT 中的 10 个实训任务（02 明确占位、其余全 PASS）。**
> 不能，就继续开发修复；能，再考虑 UI 美化与扩展。

---

*本 Prompt 由需求澄清后定稿，任何 AI 拿到此文档即可开工，无需再次询问需求。*

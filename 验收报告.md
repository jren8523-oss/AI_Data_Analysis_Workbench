# AI 数据分析工作台 · 验收报告

**验收日期**：2026-08-31
**验收方式**：交互工作台专项验收（JS 引擎冒烟 45 项 + 交互流程冒烟 16 项）＋ Python 侧全量构建验收（QA 三层 78 项）

---

## 一、验收结论

| 验收项 | 结果 |
|---|---|
| **交互工作台（新）** | ✅ 三步向导（数据源→分析类型→结果）跑通；8 项预设能力全可用；10 示例库全链路冒烟 **45+16 项全 PASS** |
| 上传数据自己跑 | ✅ CSV 上传/拖放 → 列类型识别 → 自动推荐分析 → 实时结果+SVG 图表+分层解读 |
| 报告导出 | ✅ 一键导出独立 HTML 报告（Blob 下载，可打印/投影，含 AI DATA ANALYSIS 头部） |
| 9 个可构建任务全流程跑通 | ✅ **9 / 9 PASS**（Python 侧共 78 项 QA 检查全部通过） |
| 任务 02（八爪鱼采集）占位正确 | ✅ 灰卡「需外部采集工具」，不伪造 PASS |
| 交付 PPT 可导出、可编辑 | ✅ 生成 `output/交付总览.pptx`（13 页），PowerPoint/WPS 可直接编辑 |
| 幂等性 | ✅ 删除 output 后全量重建成功，结果一致 |

> **一句话结论：工作台已从「只读报告墙」升级为「上传数据自己跑」的独立工具，十个实训作为预设功能与验收手段，达到验收标准。**

---

## 二、交付物清单

| 交付物 | 路径 | 说明 |
|---|---|---|
| **交互工作台** | `index.html` | 单文件全内联（约 2100 行 / 130KB），三步向导 + 8 项分析能力 + 10 示例库 + 报告墙，双击即开 |
| 工作台冒烟测试 ×2 | `tests/smoke_ui.js` / `smoke_interact.js` | JS 引擎 45 项 + 交互流程 16 项，`node tests/smoke_ui.js` 一键回归 |
| 任务分析报告 ×9 | `output/task{01,03..10}/report.html` | 每份含：需求 → 数据概况 → 指标计算（四件套）→ 图表 → 分层结论 → 推荐 Prompt → QA 验证 |
| 图表 PNG ×20 | `output/taskNN/charts/` | 16:9 / 150dpi / 中文字体，直接可投影 |
| 交付总览 PPTX | `output/交付总览.pptx` | 13 页可编辑，独立于课程 PPT |
| 构建日志 | `output/build_log.json` | 全量 QA 逐项记录 |
| 示例数据 ×9 | `data/raw/taskNN.csv` | UTF-8-sig，固定种子 `20240831` 可复现 |
| 一键重建 | `一键生成全部报告.bat` | 双击重跑全流程（含 PPT 导出） |
| 使用说明 | `README.md` | 快速开始 / 三步向导 / 数据口径 / FAQ |

---

## 三、系统架构

工作台由两条并行链路构成：**交互工作台（浏览器端）** 与 **批量报告管线（Python）**，共用同一套教材统计口径（双引擎口径一致）。

```
┌─ 交互工作台（浏览器端，双击 index.html 即开，全离线零依赖）──┐
│  index.html：三步向导（数据源→分析类型→结果与解读）            │
│    ├ 内联 JS 确定性引擎（parseCSV→parseNum→8 项分析 run）      │
│    ├ 内联 SVG 图表库（直方/箱线/折线/饼/预测线 8 函数）        │
│    ├ SAMPLES 示例库（10 份，与 data/raw 同源同口径）            │
│    └ 报告导出（Blob 下载独立 HTML）+ 报告墙（output 链接）      │
│  ⇄ tests/smoke_ui.js（45 项引擎冒烟）                          │
│    tests/smoke_interact.js（16 项交互流程冒烟）                │
└────────────────────────────────────────────────────────────────┘
        │ 同一套教材口径：ddof=1 / z=1.96 / 拉氏×帕氏 / IQR / 几何平均
        ▼
index.html ──► output/taskNN/report.html（9 份，静态 HTML + 内嵌图表）
                     ▲
        scripts/run_all.py（全量编排，02 跳过）
                     ▲
        tasks/taskNN_*.py（薄编排层：读数据 → 调引擎 → 组区块）
                     ▲
        engine/*.py（确定性计算引擎，LLM 零心算）
                     │
        data/raw/*.csv（只读，QA 校验 SHA-256）
```

关键设计：
- **确定性计算**：统计指标全部由 pandas / numpy / scipy 计算；LLM 只负责任务识别、方法选择、结果解释；
- **DIG 分析框架**：D 描述数据 → I 挖掘计算 → G 分层结论（Why → What → How）；
- **公式四件套**：每个指标 = 公式 → 代入 → 结果 → 解释；
- **QA 三层验证**：数值复算（独立代码路径，容差 1e-6）/ 逻辑检查（任务定制断言）/ 输出一致性（PNG 非空 + 关键元素渲染检查）；
- **报告对齐参考 PPT 风格**：顶部「AI DATA ANALYSIS」标签、人邮蓝 `#005DA2`、编号步骤卡、推荐 Prompt 框、底部小结条。

---

## 四、10 任务逐项验收结果

| 任务 | 主题 | 章节 | QA | 结果 | 关键能力验证 |
|---|---|---|---|---|---|
| 01 | 店铺访客数分析 | 第1章 | 8/8 | ✅ | 环比增速表 + 对比/趋势/占比三种分析思维（断言） |
| 02 | 使用八爪鱼采集招聘数据 | 第2章 | — | ⛔ 占位 | 灰卡：需外部采集工具，不伪造 PASS |
| 03 | 数据清洗与加工 | 第3章 | 8/8 | ✅ | 7 条清洗规则全记录、缺失/异常/重复/脏名处理、前后对比 |
| 04 | 生产资料市场价格分析 | 第4章 | 7/7 | ✅ | 集中趋势 3 + 离散 6 + 形态 2 共 11 指标全四件套 |
| 05 | 小区居民用电分析 | 第5章 | 9/9 | ✅ | 均值区间 x̄±z·(s/√n)、比例区间、大样本判定、np̂≥5 检验 |
| 06 | 产品总成本变动分析 | 第6章 | 8/8 | ✅ | 拉氏/帕氏四指数、加权算术/调和平均、指数体系自洽断言 |
| 07 | 研发与销售关系分析 | 第7章 | 8/8 | ✅ | r 分级、最小二乘回归、R²=0.969、预测 x=15 万元、相关≠因果警示 |
| 08 | 分析并预测企业总产值 | 第8章 | 9/9 | ✅ | 发展水平/速度、几何平均、趋势方程、2025 预测、外推警示 |
| 09 | 网店访客数可视化展现 | 第9章 | 11/11 | ✅ | 排序表/分类汇总/数据透视表（导出 CSV）/柱折饼/透视图，与 01 区分断言 |
| 10 | 制作销售数据分析报告 | 第10章 | 10/10 | ✅ | 分年度→比较→汇总→核心发现（事实/分析/推测/建议）→结论 |

**合计：78 项 QA 检查全部通过（9 任务 PASS + 02 占位）。**

---

## 五、硬性约束落实情况

| 约束 | 落实方式 |
|---|---|
| LLM 不心算 | 所有指标确定性代码计算，QA 数值复算独立路径比对 |
| 原始数据只读 | 每任务 SHA-256 哈希校验「构建前 = 构建后」 |
| 公式四件套 | report.formula_box 统一渲染（公式→代入→结果→解释） |
| 相关 ≠ 因果 | 任务 07 独立警示框 + 结论措辞约束 |
| 统计口径对齐教材 | Python + JS 双引擎同口径：ddof=1、众数取最小、拉氏/帕氏、z=1.96、几何平均发展速度、IQR 异常值 |
| JS 侧 QA | `node tests/smoke_ui.js`（45 项）+ `smoke_interact.js`（16 项）一键回归：指数体系自洽（relErr<1e-9）、parseNum 严格数字格式、导出报告结构、8 个 SVG 图表函数 |
| 报告风格对齐参考 PPT | AI DATA ANALYSIS 标签 / DIG 框架 / 步骤卡 / Prompt 框 / 小结条 |
| 区分度 | 任务 01（环比+三思维）vs 任务 09（排序+汇总+透视）断言互斥 |
| 中文可用 | matplotlib 自动加载微软雅黑，CSV UTF-8-sig |

---

## 六、已知限制与后续优化

1. **示例数据为构造数据**：按课程文字描述构造（报告中已标注），替换真实数据需保持列名一致；
2. **任务 02 为占位**：八爪鱼采集需外部工具与网络，工作台以灰卡提示，不提供假结果；
3. **图表为静态 PNG**：如需交互式图表（tooltip/hover）可作为后续增强（当前对齐"可投影"定位）；
4. **统计口径可扩展**：当前覆盖教材主口径，后续可加指数平滑、多元回归、卡方检验等模块（每次迭代 1 个）；
5. **残缺日期按文本处理**：如 `2024-2-30` 这类残缺日期会被 parseNum 的严格数字格式规则识别为文本列而非缺失值（防 parseFloat 截断误判），如需专门日期校验/规范化可作为后续增强；
6. **真实浏览器交互验证待做**：引擎层与交互层已由 61 项 node 冒烟覆盖，真实浏览器（拖放上传、点击热区、滚动）截图验证需安装 agent-browser（约 500MB Chromium），可按需补充；
7. **交付 PPT 为模板化导出**：如需更精细的每页版式，可在 `engine/ppt_out.py` 中扩展。
